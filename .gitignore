# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Built skill zips (users build these on demand per platform)
*.zip

# Per-project workspace artifacts users may create locally
references/
clips/
audio/
prompts/
qc-reports/

# Per-project filled-in bibles + shot list + generation log at the workspace root
# (the templates in templates/ are committed; the per-project filled versions are not)
/character-bible-*.md
/environment-bible-*.md
/prop-bible.md
/shot-list.md
/generation-log.md
